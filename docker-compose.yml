services:
  api:
    build:
      context: ./server
    container_name: telectrl-api
    env_file:
      - .env
    volumes:
      - telectrl_data:/app/data
    # expose is internal-only (other services in the same compose network can reach it).
    expose:
      - "8787"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:8787/api/health').then((r)=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: telectrl-redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - telectrl_redis:/data
    # expose is internal-only (other services in the same compose network can reach it).
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    restart: unless-stopped
    profiles:
      - redis

  web:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: telectrl-web
    depends_on:
      api:
        condition: service_healthy
    # ports publishes container port to host, so browser/users can access web UI.
    ports:
      - "${WEB_PORT:-8080}:80"
    restart: unless-stopped

volumes:
  telectrl_data:
  telectrl_redis:
